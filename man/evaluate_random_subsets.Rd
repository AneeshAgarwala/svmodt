% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svmodt_random_feature_sampling.R
\name{evaluate_random_subsets}
\alias{evaluate_random_subsets}
\title{Evaluate Multiple Random Feature Subsets Using SVM Information Gain}
\usage{
evaluate_random_subsets(
  data,
  predictors,
  response,
  n_subsets = 5,
  subset_size = 4,
  metric = c("entropy", "gini"),
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing predictors and the response variable.}

\item{predictors}{Character vector of available predictor names.}

\item{response}{Character string specifying the response variable name.}

\item{n_subsets}{Integer; number of random feature subsets to evaluate.}

\item{subset_size}{Integer; number of features in each subset.}

\item{metric}{Impurity measure for information gain. One of \code{"entropy"} or \code{"gini"}.}

\item{verbose}{Logical; if \code{TRUE}, prints evaluation progress.}
}
\value{
A data frame with two columns:
\describe{
  \item{features}{List column containing character vectors of feature names.}
  \item{info_gain}{Numeric vector of information gain values.}
}
The data frame is sorted in descending order by information gain.
}
\description{
Generates and evaluates multiple random feature subsets, ranking them by
the information gain achieved through SVM-based splits.
}
\details{
This function randomly samples \code{n_subsets} different combinations of
\code{subset_size} features from the predictor pool, evaluates each subset
using \code{\link{svm_info_gain}}, and returns them ranked by performance.

If \code{subset_size} is greater than the number of available predictors,
it is automatically reduced to match the predictor count.
}
\keyword{internal}

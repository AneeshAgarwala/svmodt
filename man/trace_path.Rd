% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svmodt_classes.R
\name{trace_path}
\alias{trace_path}
\alias{trace_path.svmodt_node}
\title{Trace the prediction path of a sample through an svmodt tree}
\usage{
trace_path(object, ...)

\method{trace_path}{svmodt_node}(object, sample_data, sample_idx = 1, ...)
}
\arguments{
\item{object}{An \code{svmodt_node} returned by \code{\link{svm_split}}.}

\item{...}{Currently unused.}

\item{sample_data}{A data frame of new predictor values (one or more rows).}

\item{sample_idx}{Integer; which row to trace (default \code{1}).}
}
\value{
Invisibly returns the predicted class label (character string).
}
\description{
Generic function that walks the tree for a single row of new data, printing
the SVM decision value and chosen branch at every internal node and the
final predicted class at the leaf.
}
\section{Methods (by class)}{
\itemize{
\item \code{trace_path(svmodt_node)}: Method for \code{svmodt_node} objects.

}}
\examples{
\donttest{
tree <- svm_split(wdbc, response = "diagnosis", max_depth = 3)
trace_path(tree, wdbc, sample_idx = 5)
}

}

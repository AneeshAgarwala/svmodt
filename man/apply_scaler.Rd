% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svmodt_apply_scaler.R
\name{apply_scaler}
\alias{apply_scaler}
\title{Apply a scaler transformation to a data frame}
\usage{
apply_scaler(df, scaler)
}
\arguments{
\item{df}{A data frame containing numeric features to be scaled.}

\item{scaler}{A scaler object with a `transform` method or function used to scale the data.}
}
\value{
A scaled data frame. If scaling fails or invalid inputs are provided,
  the original (unscaled) data frame is returned.
}
\description{
This internal helper function applies a scaling transformation to a data frame
using a provided scaler object. It returns the unscaled data in case of failure.
}
\details{
This function is intended for internal use within the package and is not exported.
It wraps the scaler's `transform()` call in error handling to prevent failures
from interrupting higher-level processes.
}
\examples{
\dontrun{
scaler <- list(transform = function(x) scale(x))
df <- data.frame(a = 1:5, b = 6:10)
apply_scaler(df, scaler)
}

}
\keyword{internal}
